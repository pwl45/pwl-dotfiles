---
alwaysApply: true
---

Err on the side of reducing complexity. Configuration should have one source of truth: when values belong to the same entity or must agree, store them together rather than in parallel structures joined by conventions, defaults, or cross-products. Don't add configuration mechanisms or abstractions for features that don't exist yet.
